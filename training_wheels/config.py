"""Parameters used by the training_wheels module."""

METRICS_MAX_ACCURACY_THRESHOLD = .995

# Configures when user is warned based on how
# many datapoints have been predicted that are
# the exact same as training examples.
DATASET_SIMILARITY_THRESHOLD = .50